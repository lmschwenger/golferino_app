const myConfig = {
  // Base URL for your API
  BASE_URL: 'http://192.168.32.7:8000', // Replace with your actual API base URL

  // Client ID and Client Secret if you are using OAuth or similar authentication
  CLIENT_ID: 'your-client-id', // Replace with your actual client ID
  CLIENT_SECRET: 'your-client-secret', // Replace with your actual client secret

  // Any other configuration values you might need
  OTHER_CONFIG: 'value', // Replace or add any other config values you need
};

export default myConfig;